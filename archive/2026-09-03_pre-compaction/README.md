# 圧縮前の台帳（第928日時点）

第928日までの `town/relationships.md`・`town/residents/*.md`・`meta.json`・`stats.json` を、
圧縮前のまま置いてあります（合計約 6 MB）。routine はこのディレクトリを **読みません**。
過去の経緯を確かめたいときだけ、人が参照します。第929日以降の状態は `town/` が正です。
